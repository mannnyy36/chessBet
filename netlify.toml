[build]
  base    = "client"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so refreshing /my-bets, /tournament/:id etc. works.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
